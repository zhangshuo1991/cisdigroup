<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.web.mapper.ITBiddingsAllNewMapper">
    <select id="getBiddingIndex" parameterType="String" resultType="java.util.Map">
        select data_id ,SUM(REPLACE(bid_amount,'万元','')) as bid_amount_single
        from cisdigroup.t_biddings_all
        where bidding_role = '招标人' and ename = #{entname}
        group by data_id
        order by SUM(REPLACE(bid_amount,'万元','')) desc
    </select>

    <select id="getMaxSupply" parameterType="String" resultType="java.util.Map">
        select eid,ename
        from cisdigroup.t_biddings_all
        where bidding_role = '中标人' and data_id = #{data_id}
    </select>
</mapper>